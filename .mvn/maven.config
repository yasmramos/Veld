# Maven Central Release Configuration

# Compiler settings for optimal performance and compatibility
-T 4C

# Clean and verbose output for release builds
-X

# Use batch mode for automated builds
-B

# Skip tests during release (tests should be run separately)
-DskipTests=false

# Enable parallel builds for faster compilation
--fail-at-end

# Use the local repository for consistency
--lax-checksums

# Additional release-specific settings
-Dproject.build.sourceEncoding=UTF-8
-Dproject.reporting.outputEncoding=UTF-8
-Dmaven.compiler.source=11
-Dmaven.compiler.target=11
-Dmaven.compiler.release=11